{
  "name": "keymap-viewer",
  "version": "1.0.0",
  "description": "Static keymap visualizer for Netlify",
  "scripts": {
    "build": "tailwindcss -i ./css/input.css -o ./css/output.css",
    "watch": "tailwindcss -i ./css/input.css -o ./css/output.css --watch",
    "serve": "http-server"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0",
    "http-server": "^14.1.1"
  }
}
